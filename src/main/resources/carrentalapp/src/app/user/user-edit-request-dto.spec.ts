import { UserEditRequestDto } from './user-edit-request-dto';

describe('UserEditRequestDto', () => {
  it('should create an instance', () => {
    expect(new UserEditRequestDto()).toBeTruthy();
  });
});
